# Plan: prj-5548-task-muhfak8d

**Project:** `PRJ-675`  
**Task ID:** `task-muhfak8d`  
**Repo:** `prj-5548-task-muhfak8d`  

## Summary

Декомпозиция задачи "Sanitize fix test" на 3 subtask: ресерч/инспекция репозитория, реализация фикса с sanitization и unit-тестами, финальная проверка и ревью.

## Subtasks

### 1. Инспекция репозитория и точка фикса

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Клонировать https://github.com/ZhenyaRUS39/prj-5548-task-muhfak8d, проверить статус git, прочитать README/README.md, найти модуль/функцию, где применяется sanitize, и зафиксировать baseline (текущее поведение + существующие тесты). Результат: краткий отчёт о том, где находится sanitize-код, какие тесты уже есть и какой baseline ожидается до фикса.
- **Test plan:** В отчёте указаны: путь к файлу(ам) с sanitize, названия существующих тестов, команда запуска тестов и их текущий exit code. git status -sb показывает clean либо список ожидаемых изменений.

### 2. Реализация sanitize-фикса и unit-тестов

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Внести минимальный фикс sanitize-логики и добавить unit-тесты, покрывающие граничные случаи (пустая строка, HTML/JS-инъекции, null/undefined, unicode). Не коммитить в main, не пушить. По завершении вернуть diff + summary.
- **Test plan:** Запустить полный тест-сьют — все тесты, включая новые, проходят (exit code 0). git diff показывает только изменения в sanitize-файле и тестовых файлах, без следов в main.
- **Dependencies:** `sub-1`

### 3. Финальная верификация и acceptance

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Прогнать тесты повторно с чистого состояния (fresh checkout), убедиться что нет регрессий, проверить покрытие новых кейсов, подготовить финальный diff + summary для передачи на ревью.
- **Test plan:** Повторный прогон тестов на свежем checkout — exit code 0. В summary перечислены: изменённые файлы, добавленные тесты, ожидаемое поведение, инструкция по воспроизведению.
- **Dependencies:** `sub-2`


---
*Generated by Hermes Orchestrator*
